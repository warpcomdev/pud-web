# Fichero para ser usado desde CLI (con kubectl -k).
# No es necesario si se utiliza la interfaz web de openshift.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - secret.yaml
  - persistentvolumeclaim.yaml